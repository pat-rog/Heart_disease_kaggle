import statistical_cal as st

st.menu()
print("Which number of column do you chose?")
choice = int(input())
if choice == 1:
    #Wynik dla pierwszej kolumny
    st.average(choice)
    st.median(choice)
    st.standard_deviation(choice)
    st.standard_error(choice)
    st.variance(choice)
    st.kurtosis(choice)
elif choice == 2:
    #Wynik dla drugiej kolumny
    st.average(choice)
    st.median(choice)
    st.standard_deviation(choice)
    st.standard_error(choice)
    st.variance(choice)
    st.kurtosis(choice)
elif choice == 3:
    # Wynik dla drugiej kolumny
    st.average(choice)
    st.median(choice)
    st.standard_deviation(choice)
    st.standard_error(choice)
    st.variance(choice)
    st.kurtosis(choice)
elif choice == 4:
    # Wynik dla drugiej kolumny
    st.average(choice)
    st.median(choice)
    st.standard_deviation(choice)
    st.standard_error(choice)
    st.variance(choice)
    st.kurtosis(choice)
elif choice == 5:
    # Wynik dla drugiej kolumny
    st.average(choice)
    st.median(choice)
    st.standard_deviation(choice)
    st.standard_error(choice)
    st.variance(choice)
    st.kurtosis(choice)
elif choice == 6:
    # Wynik dla drugiej kolumny
    st.average(choice)
    st.median(choice)
    st.standard_deviation(choice)
    st.standard_error(choice)
    st.variance(choice)
    st.kurtosis(choice)
elif choice == 7:
    # Wynik dla drugiej kolumny
    st.average(choice)
    st.median(choice)
    st.standard_deviation(choice)
    st.standard_error(choice)
    st.variance(choice)
    st.kurtosis(choice)
elif choice == 8:
    # Wynik dla drugiej kolumny
    st.average(choice)
    st.median(choice)
    st.standard_deviation(choice)
    st.standard_error(choice)
    st.variance(choice)
    st.kurtosis(choice)
elif choice == 9:
    # Wynik dla drugiej kolumny
    st.average(choice)
    st.median(choice)
    st.standard_deviation(choice)
    st.standard_error(choice)
    st.variance(choice)
    st.kurtosis(choice)
elif choice == 10:
    # Wynik dla drugiej kolumny
    st.average(choice)
    st.median(choice)
    st.standard_deviation(choice)
    st.standard_error(choice)
    st.variance(choice)
    st.kurtosis(choice)
elif choice == 11:
    # Wynik dla drugiej kolumny
    st.average(choice)
    st.median(choice)
    st.standard_deviation(choice)
    st.standard_error(choice)
    st.variance(choice)
    st.kurtosis(choice)
elif choice == 12:
    # Wynik dla drugiej kolumny
    st.average(choice)
    st.median(choice)
    st.standard_deviation(choice)
    st.standard_error(choice)
    st.variance(choice)
    st.kurtosis(choice)
elif choice == 13:
    # Wynik dla drugiej kolumny
    st.average(choice)
    st.median(choice)
    st.standard_deviation(choice)
    st.standard_error(choice)
    st.variance(choice)
    st.kurtosis(choice)
elif choice == 14:
    # Wynik dla drugiej kolumny
    st.average(choice)
    st.median(choice)
    st.standard_deviation(choice)
    st.standard_error(choice)
    st.variance(choice)
    st.kurtosis(choice)
else:
    print("Error")





